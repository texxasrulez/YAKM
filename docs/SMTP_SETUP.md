# SMTP Setup

- In Admin → Settings, enable SMTP and fill Host/Port/User/Pass and security (tls/ssl).
- `FROM_EMAIL` and `FROM_NAME` define the envelope identity.
- Use **SMTP Test** to send a themed multipart test. Check provider logs if not received.
